name: Test github actions

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo the message
        run: |
          echo "Hello World"
        