name: Docker course with react

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
      run: |
        echo "Hello World"